const AuthPage = () => {
  return (
    <p>
      This is a route group example. Since (dashboard) is stated within
      parentheses, the Auth page can be accessed directly in the URL and
      dashboard will not show up/be needed within the browser route. eg:
      domain.com/auth.
    </p>
  );
};
export default AuthPage;
